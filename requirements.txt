# =====================================================
# Robot Tự Động Bảng O2 — Robocon Bắc Ninh 2026
# Môi trường: Raspberry Pi 4 Model B + Raspberry Pi OS
# =====================================================
#
# QUAN TRỌNG: Cài gói hệ thống trước khi chạy pip!
#
#   sudo apt update
#   sudo apt install -y python3-lgpio python3-gpiozero python3-spidev \
#       python3-picamera2 python3-libcamera \
#       python3-opencv python3-numpy
#
# Bật SPI + Camera:
#   sudo raspi-config -> Interface Options -> SPI    -> Enable
#   sudo raspi-config -> Interface Options -> Camera -> Enable
#   sudo reboot
# =====================================================

# --- GPIO + SPI ---
gpiozero>=2.0
spidev>=3.6
# lgpio — KHÔNG cài qua pip, dùng apt: sudo apt install python3-lgpio

# --- Camera & nhận diện màu ---
# picamera2 — KHÔNG cài qua pip, dùng apt: sudo apt install python3-picamera2
numpy>=1.24
opencv-python-headless>=4.8.0.74

# --- Giao diện debug (chỉ dùng khi luyện tập) ---
flask>=3.0
